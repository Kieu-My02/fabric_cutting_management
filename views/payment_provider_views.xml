<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="payment_provider_form_vnpay" model="ir.ui.view">
        <field name="name">payment.provider.form.vnpay</field>
        <field name="model">payment.provider</field>
        <field name="inherit_id" ref="payment.payment_provider_form"/>
        <field name="arch" type="xml">
            <xpath expr="//group[@name='provider_credentials']" position="inside">
                <field name="vnpay_tmn_code" invisible="code != 'vnpay'"
                       required="code == 'vnpay' and state != 'disabled'"/>
                <field name="vnpay_hash_secret" invisible="code != 'vnpay'"
                       password="True"
                       required="code == 'vnpay' and state != 'disabled'"/>
            </xpath>
        </field>
    </record>
</odoo>